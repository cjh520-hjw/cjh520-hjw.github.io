name: Deploy Hugo site to Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: falseHEAD
jobs:
  build:
    runs-on: ubuntu-24.04  # 明确指定新版Ubuntu
    steps:

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.147.9
    steps:
      - name: Install Hugo CLI
        run: |
          wget -O ${{ runner.temp }}/hugo.deb https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.deb \
          && sudo dpkg -i ${{ runner.temp }}/hugo.deb
          
      - name: Install Dart Sass
        run: npm install -g sass  # 改用npm安装，避免snap不可用

 f80a09713928e6705c5e0b3b617e78764146e4cb
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

 HEAD
      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: "0.147.9"
          extended: true

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.npm
            node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies
        run: |
          npm install -g sass
          [ -f package-lock.json ] && npm ci || true

      - name: Build

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v4

      - name: Install Node.js dependencies
        run: "[[ -f package-lock.json || -f npm-shrinkwrap.json ]] && npm ci || true"

      - name: Build with Hugo
        env:
          HUGO_ENVIRONMENT: production
          HUGO_ENV: production
 f80a09713928e6705c5e0b3b617e78764146e4cb
        run: |
          hugo \
            --gc \
            --minify \
 HEAD
            --baseURL "https://cjh520-hjw.github.io/"

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3  # 必须使用v3

            --baseURL "${{ steps.pages.outputs.base_url }}/"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3  # 从v2升级到v3
 f80a09713928e6705c5e0b3b617e78764146e4cb
        with:
          path: ./public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-24.04
    needs: build
    steps:
      - name: Deploy
        id: deployment
 HEAD
        uses: actions/deploy-pages@v4

        uses: actions/deploy-pages@v4  # 从v3升级到v4
 f80a09713928e6705c5e0b3b617e78764146e4cb
